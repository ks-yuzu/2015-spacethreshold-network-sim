#pragma once

#include <string>
#include "pos.h"

static const std::string title = "20150617_Simurator";

static const Pos windowSize(960, 672);
static const Pos windowPos(30, 20);
//static const int bitDepth		= 32;

static const Pos mapSize = windowSize;
